node_modules/
dist/
.vite/
build/
coverage/
.DS_Store
*.log
.env*
!.env.example
.neurona_api_keys.json
*.mp4
*.mp3
*.wav
sqlite.db*
app_database.db*
# outputs/ adalah direktori kerja runtime (database + hasil render).
# Isinya dibuat ulang otomatis saat boot, jadi tidak ada yang perlu di-commit.
# Sebelumnya hanya sebagian pola yang di-ignore, sehingga frame hasil uji ikut
# ter-commit ke repo.
outputs/

# Per-installation AES master key (generated at runtime, never commit)
.neurona_master_key
